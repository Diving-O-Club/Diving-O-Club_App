services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    depends_on:
      - backend
    restart: unless-stopped
